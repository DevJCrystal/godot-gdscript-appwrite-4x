# Copy this file to `.env` and fill in your values.

# --- Core (required) ----------------------------------------------------------

# Your Appwrite Project ID (from the Appwrite Console)
APPWRITE_PROJECT_ID=

# Your Appwrite endpoint
# Cloud example:
APPWRITE_ENDPOINT=https://nyc.cloud.appwrite.io/v1

# --- TLS / local dev ----------------------------------------------------------

# If you're running against a self-signed Appwrite instance, set this to true.
APPWRITE_SELF_SIGNED=false

# --- Debug flags (optional) ---------------------------------------------------

# Persist the cookie jar to disk (reduces repeated logins while iterating)
APPWRITE_DEBUG_PERSIST_SESSION=false

# Print HTTP requests/responses from the REST client
APPWRITE_DEBUG_HTTP=false

# Print realtime WebSocket frames/state transitions
APPWRITE_DEBUG_REALTIME=false

# Security: API keys are opt-in.
# This SDK is intended to be client-safe (no embedded key).
# Only set these for trusted/server-side tooling.
# APPWRITE_ENABLE_API_KEY=false
# APPWRITE_KEY=

# --- IDs used by tests/examples ----------------------------------------------

APPWRITE_DATABASE_ID=
APPWRITE_TABLE_ID=

APPWRITE_FUNCTION_ID=
APPWRITE_STORAGE_BUCKET_ID=

# Test account (used by tests that login)
# Use a dedicated test user.
APPWRITE_TEST_EMAIL=
APPWRITE_TEST_PASSWORD=

# --- Realtime (optional) ------------------------------------------------------

# For examples/realtime_monitor.tscn you can either provide an explicit channel:
# APPWRITE_REALTIME_CHANNEL=databases.<db>.collections.<table>.documents
# Or rely on APPWRITE_DATABASE_ID + APPWRITE_TABLE_ID above.
APPWRITE_REALTIME_CHANNEL=

# Only set this if you *must* send a specific Origin header.
# Leaving it empty is usually best for native Godot clients.
APPWRITE_REALTIME_ORIGIN=

# --- Optional test flags ------------------------------------------------------

APPWRITE_TEST_PRINT_DOCS_JSON=false

# Realtime test timing controls (tests/test_realtime.gd)
APPWRITE_REALTIME_WAIT_FULL=false
APPWRITE_REALTIME_WAIT_SECONDS=10
APPWRITE_REALTIME_CONNECT_WAIT_SECONDS=5
# APPWRITE_TEST_CLEAR_COOKIES=false
# APPWRITE_TEST_LOGOUT=false
# APPWRITE_E2E_TEST_REUSE_SESSION=false
